function name()
{
    const names = ['zakir','QuaidEAzam'];

    function safety()
    {
        for(let heroes of names)
        {
            console.log(`Our Heroes : ${names}`)
        }
        
    }

    safety();
}

name();